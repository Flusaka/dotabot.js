name: Test on PR
on: pull_request
jobs:
  test:
    name: test
    permissions:
      contents: read
    runs-on: ubuntu-latest
    steps:
      # ...
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - run: bun install
      - run: bun run test
      
